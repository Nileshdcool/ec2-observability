// Enum for waste filter options
export enum WasteFilter {
  All = "All",
  Underused = "Underused",
  OverProvisioned = "Over-provisioned",
  OK = "OK"
}

// Enum for time range options
export enum TimeRange {
  OneDay = "1d",
  SevenDays = "7d",
  FourteenDays = "14d"
}
